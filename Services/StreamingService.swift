import Foundation
import WebRTC

class StreamingService {
    static let shared = StreamingService()

    func startStreaming() {
        print("Starting streaming...")
        // Initialize WebRTC streaming session
    }

    func stopStreaming() {
        print("Stopping streaming...")
    }
}

